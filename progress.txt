#2: Scaffolded obsi.html by copying spaceInvaders.html and stripping out barriers, UFO, multi-input switching, keyboard directional movement, and marching sounds. Wired up engineInit callbacks with blank playfield rendering at 960x540 with black background and HUD placeholders for score, lives, and level. Player renders but has no input handling yet.

#3: Implemented auto-movement and basic shooting. Player ship auto-moves horizontally at fixed speed, bouncing off left and right edges. Auto-shoots bullets upward every 20 frames. Bullets travel upward and destroy when off-screen. No keyboard/mouse input affects movement.

#4: Implemented full Player FSM with 6 states (moving, charging, charged, speed_burst, burnout, pacifist) and Magic Combo input system. Mouse click controls: tap reverses direction, hold to charge (slows to 0.2x speed), charged state at 1s (resumes 1x speed), release charged for charged shot + speed burst (4x speed), hold too long triggers burnout (2x speed, locked input). Uses LittleJS Timer class for timed transitions and velocity acceleration/deceleration.

#5: Implemented weapon temperature system. Auto-shooting raises temperature 10 per shot, passive cooling 0.6 per frame. Warm zone (40-70 temp) gives fire-rate bonus. Overheat at 100+ temp triggers burnout state automatically. Added HUD temperature bar with color gradient: blue=cool, orange=warm, red=overheat.

#6: Implemented Wave FSM with transition/combat/boss_defeated/cycle_transition states. Wired Wave FSM to Player FSM: wave start puts player in pre_entry, immediately transitions to pacifist/moving. Wave clear triggers player exiting. Wave counter increments, boss waves every 10th wave (10, 20, 30...). Added wave number to HUD with boss label.

#7: Implemented Shooter enemy, ENEMY_CONFIGS object, and difficulty scaling. Created calcDifficulty(waveN) function for wave-based scaling. Spawns 5x11 grid of shooters that move side-to-side, drop on wall, speed up as count falls. Shooters fire downward from column lowest alive. Bullet-enemy collision detection with score awarding. Formation speed scales with difficulty.

#8: Implemented Diver enemy with diving behavior. Divers sit in formation (odd rows), periodically break out and dive toward player position. Return to original formation slot after reaching player Y level. Diver-player collision causes life loss. Dive chance and speed scale with difficulty. Mixed Diver (cyan) and Shooter (red) grid formation.

#9: Implemented Reflector and Absorber enemies. Reflector (yellow) reflects player bullets downward as enemy projectiles and takes 3 hits to destroy. Absorber (dark blue) absorbs bullets up to maxStored, displays count, spits in spread pattern on maxStored or spitDelay. Spawning: row 0=shooter, 1=diver, 2=reflector, 3=absorber, 4=shooter. Updated scoring to reflect each enemy type value.

#10: Implemented Treasure mini-boss. Spawns every 30s during combat, bounces diagonally around screen with pool-ball physics, high health (50), awards large score bonus (500) on defeat. Only one treasure active at a time, does not count toward wave clear. Distinctive gold sprite.

#11: Implemented Boss wave (v1 stub). Every 10th wave spawns single Boss instead of grid. Boss moves side-to-side, fires 3-bullet spread downward. Health and fire rate scale with cycle number (every 10 waves). Boss defeat triggers celebration → cycle_transition → next cycle. Difficulty increases each cycle. Game loops indefinitely through 10-wave cycles.

#12: Added Space bar keyboard alias for Magic Combo input. Extracted three helper functions (btnPressed, btnReleased, btnDown) that check both mouse (left-click) and keyboard (Space) input. Updated Player FSM methods (updateMoving, updateCharging, updateCharged) to use helpers instead of raw mouse calls. Space bar now triggers charge on press, reverses direction on tap, fires charged shot on release, and triggers burnout on held-too-long, with mouse left-click behavior unchanged.

#13: Implemented player entry animation. Added entryTimer to Player constructor. Spawn player off-screen at (-2, playerY) on game start and at each wave transition (in pre_entry state). UpdatePreEntry lerps position from -2 to playerY over 60 frames using timer progress. On completion, snap to final position and transition to pacifist state. Player slides smoothly up from below screen at game start and each new wave.

#14: Fixed player ship border collision behavior. Modified clampToPlayfield() to flip dirX and negate targetVelX when ship hits left or right playfield borders. Now the ship bounces away from walls instead of sticking to them. Acceleration system drives ship away from border in all states (moving, charging, charged, speed_burst, burnout).

#15: Refactored monolithic obsi.html into 6 focused JS modules: constants.js (world-unit-commented game config, PLAYER_CONFIG object, grid layout derived from 85% play-area width, temperature thresholds, ENEMY_CONFIGS with values in seconds), objects.js (RectObject base class), bullets.js (Bullet/ChargedBullet/EnemyBullet classes), enemies.js (Shooter/Diver/Reflector/Absorber/Boss/Treasure enemy classes), player.js (Player state machine with fixed entry animation using Timer.getPercent() instead of manual frame math), game.js (game loop, global state, difficulty scaling, formation/wave FSM, HUD rendering). Converted all frame-count timers to LittleJS Timer objects: cooldown/chargeTimer/burnoutTimer/speedBurstTimer/pacifistTimer (Player), shootTimer (Shooter/Boss), diveRateTimer (Diver), spitTimer (Absorber), formationShootTimer, treasureSpawnTimer. Grid positioning now scales with LEVEL_SIZE instead of hardcoded spacing. Tests pass, eslint clean.

#16: Fixed player entry sequence double-entry bug and exiting state premature transition. Bug 1: gameInit() was leaving waveState='transition' instead of setting to 'combat' and spawning enemies immediately, causing wave FSM to reset player 2s after entry animation started. Fixed by setting waveState='combat', calling spawnEnemyGrid(), and initializing treasureSpawnTimer in gameInit(). Bug 2: updateExiting() was auto-transitioning to 'moving' after 30 frames (before wave FSM's 2s transition timer), leaving player active with no enemies on screen. Fixed by removing self-transition block — player now freezes in 'exiting' state until wave FSM resets it during transition. On game load: player slides in with enemies already in formation, no double entry. After wave clear: player freezes, new wave spawns, player re-enters normally.

#18: Removed auto-shooting and cooldown system from Player FSM. Deleted cooldown timer initialization from constructor, removed entire auto-fire block from updateMoving() state (cooldown check → bullet creation → temperature increment → cooldown reset), removed cooldown.set(0) from updatePacifist(), removed cooldownFrames/cooldownMin/cooldownBonus config parameters from PLAYER_CONFIG, and removed TEMP_WARM_MIN/TEMP_WARM_MAX constants from constants.js. Updated game.js HUD temperature bar color thresholds to use hardcoded values (40, 70) instead of removed constants. Magic Combo gestures (direction reversal, charging, charged release, speed burst, burnout) remain fully functional. No bullets fire automatically in moving state. Temperature system and overheat threshold intact. Tests pass, eslint clean.

#19: Added tap-release bullet firing to Magic Combo. In charging state, when button is released before charge threshold (1s), player now fires a normal Bullet with same spawn point and velocity as former auto-shoot, increments temperature by TEMP_PER_SHOT, and reverses direction. Charged release (held ≥1s) unchanged: fires ChargedBullet + speed burst, no extra normal bullet. Temperature accumulation on tap-firing enables rapid-tap overheat trigger (10 taps = 100 temp = burnout). Pacifist and pre-entry states unaffected (no tap-fire). Tests pass, eslint clean.

#20: Updated CONTEXT.md and wrote ADR-0006 to document tap-to-shoot design. CONTEXT.md: updated Magic Combo definition to include tap-release mechanics (fires bullet + reverses direction), marked Auto-Shoot obsolete with removal reference (#18), added Weapon Temperature entry explaining heat system (TEMP_PER_SHOT per shot, TEMP_COOLING_RATE passive cooling, TEMP_OVERHEAT threshold as sole spam limiter, no warm-zone bonus). ADR-0006 covers decision rationale (every action now tactically input-driven), why surprising (tap previously non-offensive), trade-offs chosen (simplified mechanic: one threshold instead of two, no cooldown), and powerup incompatibility note (rapid-fire powerups incompatible; future powerups should be effect-based: pierce/spread/damage/homing). Tests pass, eslint clean.

#21: Replaced screen-space HUD temperature bar with diegetic gun barrel on player ship nose. Removed renderShootTemperature() function and renderTemperature flag from game.js. Added getBarrelColor() method to Player class that interpolates barrel color in two segments: 0–40 temp green→yellow, 40–100 temp yellow→red, using LittleJS rgb() and lerp() utilities. Barrel renders as narrow rect at ship nose (pos + vec2(0, 0.5), barrel width 0.15, height 0.6) in gameRender() via Player.render(). Always visible, changes color as weapon temperature rises and cools. No numeric label or screen-space indicator remains. Tests pass, eslint clean (player.js, game.js).

#23: Implemented Wave DSL parser and wave definitions. Created waves.js with WAVE_DEFINITIONS array of 10 pre-defined waves (9 regular + 1 boss) encoded as pipe-delimited DSL strings. DSL format: ASCII_GRID|pattern_code|cleared_msg,prepare_msg. parseWaveDSL() function validates and parses each wave, extracting entities (2D char array), pattern metadata (order/entry/base/inc), and interwave messages. Supports 3 spawn patterns: c=row_major/from_top, l=col_major/alternating, s=spiral/random. Grid fixed at 5 rows × 11 cols. Loaded waves.js before game.js in obsi.html. All tests passing, no linting errors in new code.

#24: Implemented buildSpawnQueue(waveDef) function in waves.js. Extracts all non-empty cells from parsed wave definition entities grid, sorts by spawn order (row_major, col_major, or spiral), assigns entry styles (resolves alternating to left/right per column parity), and computes spawn timers. Spiral sort uses Manhattan distance from grid center (5, 2). Timer stagger: (base + index*inc)/60 to convert pico-8 frames to seconds.

#25: Implemented enemy entry fly-in animation. Added isEntering property, entryStyle, gridPos, entryStartPos, and entryTimer to all enemy classes (Shooter, Diver, Reflector, Absorber). computeOffScreenPos() method calculates spawn position based on entry style (from_top, from_left, from_right, or random). During entering, enemies lerp from off-screen start to grid position over 0.4s, suppressing shooting/diving behavior. Entry completes when timer elapsed, setting isEntering=false and locking position at grid location.

#26: Integrated spawn queue into game loop. Replaced hardcoded spawnEnemyGrid() with DSL-driven spawning: wave start now parses WAVE_DEFINITIONS[waveIndex], calls buildSpawnQueue() to populate spawnQueue global. processSpawnQueue(dt) called during combat state each frame, decrementing spawn timers and calling spawnEnemy() dispatcher when ready. spawnEnemy() maps char-to-class (p/P=Shooter, k/K=Diver, r/R=Reflector, a/A=Absorber, B=Boss, g=Treasure, E=fallback-Shooter), constructs enemy with isEntering=true and entryStyle. Wave selection cycles through 10 definitions modulo waveN.

#27: Implemented elite enemy variants and reworked absorber spit behavior. Added elite configs to ENEMY_CONFIGS: shooterElite (2HP, aimed=true), diverElite (4HP, faster diveSpeed/Rate), reflectorElite (5HP), absorberElite (2HP, spread=true, faster spitDelay). Each enemy class constructor now detects isElite flag and reads appropriate config. spawnEnemy() passes isElite=true for uppercase chars (P, K, R, A). Normal absorber spits one bullet down per spitDelay interval; elite spits 3-bullet spread simultaneously. Diver now stores diveSpeed/diveRate instance vars for elite scaling.

#28: Implemented interwave text overlay. Added currentClearedMsg and currentPrepareMsg globals to store transition messages. spawnEnemyGrid() captures cleared/prepare strings from parsed wave definition. During transition waveState, gameRenderPost() renders both messages centered on screen: cleared message (bright green) above center, prepare message (white) below center. Messages display for entire 2s transition duration, disappear when combat begins.

#30: Added Vitest smoke tests for parseWaveDSL. Moved WAVE_DEFINITIONS and parseWaveDSL function from waves.js to constants.js with ES module exports. Added minimal Vector2 polyfill to constants.js for test environment (if typeof Vector2 is undefined, defines class Vector2 and vec2() function on globalThis—will be overridden by LittleJS in browser). Created prototypes/obsi/tests/smoke.test.js with two test cases: (1) valid DSL string parses to object with correct entities grid (5 rows, 11 cols), pattern metadata, and interwave messages; (2) empty/blank input returns without throwing and yields empty entities array and null pattern. All tests passing, eslint clean on modified source files.

#31: Added Vitest smoke tests for buildSpawnQueue. Moved buildSpawnQueue function from waves.js to constants.js with ES module export. Added tests to smoke.test.js verifying: (1) non-empty queue returned for valid wave definition, (2) each queue entry has col, row, eType fields with correct types, (3) row_major ordering respected (wave 0 first entry at row 0, col 1). All tests passing, eslint clean on modified source files.

#32: Added Vitest smoke tests for calcDifficulty. Moved calcDifficulty function from game.js to constants.js with ES module export. Refactored function to use BASE_FORMATION_SPEED constant (0.08) instead of mutable global formationSpeed variable, removing circular dependency and making function testable. Added tests to smoke.test.js verifying: (1) wave 1 returns baseline formationSpeed (~0.0128), (2) higher wave numbers return larger multipliers, (3) waves 1-20 return no NaN or Infinity values. All tests passing, eslint clean on modified source files.

#33: Added computeHitEvent pure function to constants.js with full Vitest test coverage. Canonical HitEvent shape: { kind, reflected, pos, scoreValue, spawned }. Function logic: newHealth = health - dmg; kind='damaged' if newHealth > 0 (scoreValue=0), kind='killed' if newHealth <= 0 (scoreValue passed through); reflected, spawned, and bulletPos always passed through unchanged. Added four test cases covering survivor hit, exact-health-to-zero kill, overkill (health < 0), and pass-through of all fields. Added HitEvent entry to CONTEXT.md glossary defining the shape, semantics, and purpose. All tests passing, eslint clean.

#34: Implemented applyDamage helper and onBulletHit methods on simple targets. Added applyDamage(target, bullet, dmg=1) function to enemies.js that wraps computeHitEvent, mutates target.health, calls bullet.destroy(), and target.destroy() on kill. Set this.scoreValue in constructors for all six target classes (Shooter, Diver, Reflector, Absorber, Boss, Treasure) reading from ENEMY_CONFIGS scores. Implemented identical onBulletHit(b) methods on Shooter, Diver, Boss, Treasure that return applyDamage(this, b). Added Vitest test for Boss.onBulletHit returning kind:'killed' with correct scoreValue when health reaches 0. All tests passing, eslint clean.

#35: Implemented Reflector.onBulletHit with reflection via HitEvent. Creates mirrored EnemyBullet with negated Y velocity at bullet position, calls applyDamage for damage/kill, sets event.reflected=true and event.spawned=[mirroredBullet], returns modified event. Coexists with existing game.js reflection loop — new method untouched by collision code until next slice. Added Vitest test for damaged Reflector (health > 0 after hit) verifying kind:'damaged', reflected:true, spawned.length===1, and mirrored Y velocity. All tests passing, eslint clean.

#36: Implemented Absorber.onBulletHit with storage and overflow paths. If absorb() succeeds, destroy bullet and return absorbed event with kind:'absorbed', reflected:false, scoreValue:0, spawned:[]. On overflow (storage full), call applyDamage(this, b) which handles damage and possible kill. spit() side-effect remains unchanged in update() method. Added two Vitest tests: (1) empty absorber returns kind:'absorbed' with no health change and storedCount incremented, (2) full absorber overflows and returns damage event (kind:'killed' with correct scoreValue when health reaches 0). All tests passing, eslint clean.

#37: Created audio.js and fx.js stub modules wired into obsi.html. audio.js exports playHitSound(ev) with empty cases for kind:'damaged'/'killed'/'absorbed' and reflected variants. fx.js exports spawnHitParticles(ev) with same switch structure. Both modules have empty case bodies — ready for real SFX/VFX wiring later. Added script tags to obsi.html in order (after player.js, before game.js) following sequential-script pattern documented in CONTEXT.md. Calling either function with any HitEvent shape returns without throwing. Existing tests pass, no regression.

#38: Collapsed three bullet-collision loops in game.js into unified HitEvent fanout. Replaced separate treasure, enemy, and boss collision blocks (lines 184-196, 198-239, 240-255) with single loop: iterate bullets, iterate all targets (enemies, boss, treasure), call target.onBulletHit(bullet), fan HitEvent to playHitSound/spawnHitParticles, accumulate score, push spawned bullets, handle boss-defeated transition. Removed all instanceof Reflector/Absorber branches and isinstance-based score lookup ladder. Kept player-diver and player-treasure collisions untouched (lines 205-235). Kept formation movement logic intact (triggered when no boss). Boss update() is auto-called by LittleJS. All tests pass, eslint clean. Unified loop enables polymorphic HitEvent-driven collision resolution.

#39: Fixed browser load ReferenceError by mirroring computeHitEvent from pure.js (ESM) into constants.js (classic script). Added classic-script function definition to constants.js after calcDifficulty, body byte-for-byte identical to pure.js version (no export keyword). Tests pass, eslint clean on modified files. Browser loads obsi.html via HTTP server without ReferenceError. pure.js unchanged.

#40: Documented pure.js↔constants.js duplication rule. Sharpened pure.js header comment with explicit RULE: every function in this file MUST be mirrored as classic-script copy in constants.js or browser ReferenceErrors. Added Module system entry to CONTEXT.md noting pure functions live in both files and must stay in sync. Tests pass.

#41: Created git subtree split branch obsi-port from prototypes/obsi/ to preserve full commit history (including #33–#40 narrative) for relocation to adamico/obsi. Executed `git subtree split --prefix=prototypes/obsi -b obsi-port`, verified log shows obsi commits at root paths with no prefix, pushed branch to origin. main branch unaffected. No file edits in this slice—pure git operation enabling clean history merge into legacy/ folder downstream.

#42: Graduated obsi from prototype phase and relocated to adamico/obsi:build/littlejs/. Deleted prototypes/obsi/ (git history preserved via obsi-port branch), created prototypes/obsi-MOVED.md pointer file linking to relocated repo. Added docs/adr/0007-obsi-exited-prototype-phase.md documenting graduation, scope change (ADR-0001 now applies only to auto-defender/autorobosurvivor), and rationale for ADR migration. Deleted ADRs 0004, 0005, 0006 from aijam3 (now maintained in obsi repo). Verified git log --oneline -- prototypes/obsi/ still resolves full 40-slice history despite directory deletion.

#53: Added a pure Spell Keeper dive state module at spellkeeper/src/diveState.js with exported default tuning knobs, deterministic start/advance/expire behavior, and locked trigger direction computed from cursorTarget relative to origin. Added focused Vitest coverage for idle state, trigger locking, fixed-duration expiry, and no-op advancement from idle. Post-push vitest gate passed on the pushed commit; issue closed with the requested ralph: done comment.

#59: Added a pure seeded Spell Keeper shot-plan generator at spellkeeper/src/shotPlan.js with a fixed three-shot opener, deterministic 30-shot plan generation, seeded phase-based variation, and readable designer metadata for index, difficulty band, opener marker, and pressure tags. Wired main.js to spawn from the generated plan, added coverage for same-seed determinism, opener stability, metadata shape, and invalid inputs, and fixed the root Vitest config so `npx vitest run` uses the existing LittleJS mock setup. Post-push gate passed with `npx vitest run` and `npx eslint spellkeeper/src`; issue #59 was closed with `ralph: done`.

#60: Extended the Spell Keeper shot-plan generator with named origin lanes, target lanes, and placement-height zones, plus seeded jitter and safe target clamping inside the goal frame. Heavy shots now stay straight with matched x positions, every shot carries lane/height metadata through to shot creation, and the test suite now checks lane metadata, clamp bounds, and hex coverage. Post-push validation passed with `npx vitest run` and `npx eslint spellkeeper/src`; issue #60 was closed with the requested completion comment.

#61: Refined the Spell Keeper shot-plan generator into three explicit difficulty acts: readable variety, mixed pressure, and chaos-but-fair. The updated pools keep early fireballs low and central, keep mixed-act fireballs high but non-wide, and allow a high-corner fireball in the final act while preserving on-target clamping, repeat avoidance, and guaranteed coverage of all current Hexes. Added coverage for the exact act labels and the fireball fairness rules; post-push validation passed with `npx vitest run` and `npx eslint spellkeeper/src`.

#62: Tightened the Spell Keeper shot-plan generator with back-to-back anti-repeat enforcement for hex, target lane, and exact shot signature matches, and redefined Heavy Hex as a low extreme-side commitment shot with matching origin/target x and explicit pressure tags. Added a deterministic weighting hook so mixed and chaos bands can bias follow-ups away from a recent Heavy shot toward the opposite side without making that pattern mandatory. Post-push validation passed with `npx vitest run` and `npx eslint spellkeeper/src`; issue #62 was closed after the completion comment.

#63: Integrated the generated Shot Plan into Spell Keeper's scene flow so game initialization rebuilds the current match plan, shot spawning reads from that plan by index, and shot progression advances through the seeded sequence after each resolution. Removed the obsolete ramp module and replaced the old ramp-focused test coverage with scene-level assertions that the active shot matches the generated plan and advances to the next entry after a shot resolves. Post-push validation passed with `npx vitest run` and `npx eslint spellkeeper/src`.

#64: Exposed a debug inspection seam for the active Spell Keeper Shot Plan via `getShotPlanDebugInfo()`, backed by a pure `describeShotPlan()` snapshot helper that returns cloned shot and designer metadata without adding any player-facing preview or seed UI. Added coverage that exercises the seam after `gameInit()` and verifies the returned data is inspectable and detached from the live plan object. Post-push validation passed with `npx vitest run` and `npx eslint spellkeeper/src`; issue #64 was closed after the completion comment.

#65: Ran the final PRD conformance pass for the seeded Spell Keeper Shot Plan after the debug seam landed. Added a scope regression test that keeps the generated plan at 30 shots and guards against accidental reintroduction of seed UI, preview UI, adaptive difficulty, trajectory-height randomization, or Familiar Trick fields. Post-push validation passed with `npx vitest run`, `npx eslint spellkeeper/src`, and `npm run build`; issue #65 was closed after the completion comment.

#67: Added a focused Spell Keeper shot runtime seam in `spellkeeper/src/shotRuntime.js` with explicit shot-dimension and timing config, immediate first-shot spawning from a supplied Shot Plan, and detached read helpers for the active shot entry, shot pose, and feedback state. Added unit coverage for successful creation, immediate availability, snapshot detachment, and missing/empty plan failures. Post-push validation passed with `npx vitest run` and `npx eslint spellkeeper/src`; issue #67 was closed after the completion comment.

#68: Moved active shot advancement and goal-plane resolution coordination into `spellkeeper/src/shotRuntime.js` as a reducer-style API that returns `{ runtime, events }`, accepts injected save-resolution seams plus Familiar save segments, and emits shot-resolved events for save/conceded outcomes without mutating match score or win/loss state. Updated `spellkeeper/src/main.js` to consume the runtime events and keep the scene state synchronized, added Vitest coverage for in-flight advancement plus save and conceded resolution paths, and verified the pushed state with `npx vitest run` and `npx eslint spellkeeper/src`. Issue #68 was closed after the completion comment.

#69: Added runtime-owned feedback and respawn scheduling for Spell Keeper shots. The shot runtime now tracks last resolution feedback, feedback expiry, scheduled next-shot index, and match-complete state so orchestration can feed the match outcome back into the runtime before the next spawn decision. Main.js now reports match state back into the runtime after each resolution, and new tests cover feedback expiration, respawn delay, next-shot spawn, and complete-match no-respawn behavior. Post-push validation passed with `npx vitest run` and `npx eslint spellkeeper/src`.

#70: Wired Spell Keeper main orchestration to the extracted shot runtime. Rendering now reads ball pose and feedback from runtime snapshots/helpers, match saves and concessions come only from match state, and shot-resolved events still drive progression through the existing match-state authority. Kept Familiar save segments flowing into runtime advancement, preserved player-facing shot/save feedback, and verified the pushed branch with `npx vitest run` plus `npx eslint spellkeeper/src`. Issue #70 is ready to close.

#71: Added regression coverage for the extracted Spell Keeper shot lifecycle at the public runtime/orchestration seams. The new tests cover first-shot spawn, advancement, save resolution, conceded handling, respawn timing, complete-match no-respawn behavior, aggregate save/concede separation, and match-state score ownership. Post-push validation passed with `npx vitest run` and `npx eslint spellkeeper/src`; no lint follow-up was needed.

#79: Reworked Spell Keeper save resolution to classify crossing-frame contact as `saved`, `deflected`, or `conceded`, with two-glove contact winning over simultaneous body contact and any actual contact beating a goal. The match layer now accepts `saved | deflected | conceded`, tracks deflections separately at 25 points, preserves clean-sheet handling for deflections, and the main feedback seam now renders SAVE, DEFLECTED, or GOAL with updated aggregate counts. Post-push validation passed with `npx vitest run` and `npx eslint spellkeeper/src`; issue #79 was closed after the completion comment.

#80: Extended Spell Keeper shot-resolution events to surface resolver fields directly on `shot-resolved` while keeping the nested `result` payload intact, so runtime consumers can read `saved | deflected | conceded` without duplicating collision logic. Updated main orchestration to fall back to the top-level event outcome, added deflected event coverage, and verified the pushed commit with `npx vitest run` and `npx eslint spellkeeper/src`.

#81: Tightened Spell Keeper match-state coverage so the score oracle now explicitly proves saved shots award 100, deflections award 25 without adding concessions, and conceded shots add zero points while still advancing the loss counter. Post-push validation on the committed branch passed with `npx vitest run` and `npx eslint spellkeeper/src`; no lint follow-up issue was needed.

#82: Split Spell Keeper match-state flags so clean sheet now depends only on zero conceded goals, while perfect control separately requires a win with zero deflections. Added explicit `cleanSheet` and `perfectControl` state fields plus helper predicates and regression coverage for the all-saves case and the mixed save+deflection clean-sheet case. Post-push validation passed with `npx vitest run` and `npx eslint spellkeeper/src`; issue #82 was closed after the completion comment.

#83: Outcome feedback now distinguishes `SAVE +100`, `DEFLECTED +25`, and `GOAL +0` at shot resolution using runtime-owned feedback state, with the main render path reading the active shot result instead of duplicating scoring authority. The score HUD and match-end banners stay separate, and the Vitest coverage already proves the save/deflection/goal labels through the public feedback seam. Post-push validation is next on the committed state, followed by the issue closeout comment.

#84: Exposed richer shot-resolution summaries for Spell Keeper debug and post-run tuning. Save resolution now carries outcome quality, score delta, and overlapping contact segment IDs/types through runtime feedback, public state snapshots, and the post-run overlay, so clean two-glove saves can be distinguished from deflections and body contact in later calibration work. Post-push Vitest passed; no separate lint remediation issue was created.

#85: Wired the public Spell Keeper orchestration summary to expose the full three-outcome flags (`isSave`, `isSaved`, `isDeflection`, `isConcession`) and fixed manual match-outcome application so runtime feedback refreshes when a new result differs from the currently displayed one. Added regression coverage for saved and conceded summary snapshots, then repushed the repair; post-push validation passed with `npx vitest run` and `npx eslint spellkeeper/src`.

#86: Added a tutorial-only shot telegraph for the first two Spell Keeper shots. The renderer now draws a temporary projected path plus target reticle for opening shots only, fades the cue out during the shot, and leaves all save logic on the existing Goal Plane body-segment overlap flow. Added pure coverage for the sampled telegraph path and runtime gating so later shots do not expose the read aid. Post-push validation passed with `npx vitest run` and `npx eslint spellkeeper/src`; no lint follow-up was needed.

#73: Added a clean-save-triggered Familiar reveal to Spell Keeper. Flashy saves now pulse glowing eyes and a tail pop during the existing feedback window, driven entirely from runtime save feedback so collision/save resolution stays unchanged. Added regression coverage for the reveal state and render tell, then verified the pushed commit with `npx vitest run` and `npx eslint spellkeeper/src`; no lint follow-up issue was needed.

#74: Added generated Spell Keeper SFX without external assets. Launch cues now fire on initial spawn and each respawn, outcome cues differ for saved/deflected/conceded results, and a separate match-end flourish plays off the same runtime/match transition path. Added focused audio and orchestration coverage, then repushed until the post-push Vitest gate passed. Lint was not run in this live session, so no lint follow-up issue was created.
